# hyprpaper — wallpaper daemon
# Wallpapers are set by hyprland-setup.sh on each boot

preload = /usr/share/backgrounds/default.png
wallpaper = ,/usr/share/backgrounds/default.png

# Unload unused to save memory
unload = all
